services:
  chirpedex:
    build:
      context: .
      dockerfile: images/identity.Dockerfile
    profiles:
      - prod
  chirpedex-test:
    build:
      context: .
      dockerfile: images/identity.Dockerfile
      target: test
    profiles:
      - testing
